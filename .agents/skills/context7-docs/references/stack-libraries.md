# Project Stack Documentation Recipes

Tailored query recipes and Context7 best practices for the CodeGrogu Portfolio tech stack.

---

## 1. Web3D & Graphics (Three.js / R3F / Drei)

### Three.js WebGPU

- **Library ID**: `/mrdoob/three.js`
- **Typical Queries**:
  - `"How to initialize WebGPURenderer with WebGL fallback in three.js"`
  - `"TSL (Three Shading Language) compute shaders and custom node materials in WebGPU"`
  - `"Post-processing pipeline with WebGPURenderer"`

### React Three Fiber & Drei

- **Library IDs**: `/pmndrs/react-three-fiber`, `/pmndrs/drei`
- **Typical Queries**:
  - `"How to manage canvas performance, frameloop demand, and canvas unmounting in R3F"`
  - `"Drei ScrollControls, Float, and Canvas responsive viewport sizing"`
  - `"Using Drei useGLTF with Draco compression and LOD (Level of Detail)"`

---

## 2. Full-Stack Data Layer (Neon & Drizzle ORM)

### Drizzle ORM & Neon Serverless

- **Library IDs**: `/drizzle-team/drizzle-orm`, `/neondatabase/serverless`
- **Typical Queries**:
  - `"How to configure drizzle-orm with neon-http driver in Next.js App Router"`
  - `"Defining PostgreSQL schemas with pgTable, uuid, timestamp, and jsonb in Drizzle"`
  - `"Drizzle relational queries vs SQL select with prepared statements"`
  - `"Handling transactions and connection pooling with Neon serverless driver"`

---

## 3. Form Validation & Server Actions (Zod)

### Zod Validation

- **Library ID**: `/colinhacks/zod`
- **Typical Queries**:
  - `"Validating Next.js Server Action form data with Zod safeParseAsync"`
  - `"Zod custom error messages, transform, and refine for booking dates"`
  - `"Inferring TypeScript types from Zod object schemas"`

---

## 4. Animation & Interactivity (GSAP & ScrollTrigger)

### GSAP

- **Library ID**: `/greensock/gsap`
- **Typical Queries**:
  - `"Using GSAP useGSAP hook in React 19 / Next.js with cleanup"`
  - `"Configuring ScrollTrigger scrub, pin, and responsive matchMedia breakpoints"`
  - `"Coordinating GSAP timeline with Three.js object transformations"`

---

## 5. Next.js App Router & Tailwind CSS

### Next.js

- **Library ID**: `/vercel/next.js`
- **Typical Queries**:
  - `"Next.js 15 metadata object, generateMetadata, and OpenGraph image generation"`
  - `"Dynamic route segment params and searchParams Promise typing in Next.js 15"`
  - `"Next.js Server Actions with useActionState and revalidatePath"`

### Tailwind CSS

- **Library ID**: `/tailwindlabs/tailwindcss`
- **Typical Queries**:
  - `"Configuring custom color variables, animations, and dark mode class strategy in Tailwind"`
  - `"Tailwind container queries and responsive typography plugins"`
